# PR triage — 2026-08-09 (G4)

**Статус сбора:** `UNVERIFIED` — `gh pr list` → HTTP 401 (invalid keyring token). Ветка **запушена** (`6e3119e`, force-with-lease 09.08).

| PR | Тема | SSOT-файлы | Решение | Обоснование | Срок |
|----|------|------------|---------|-------------|------|
| **NEW** | P0 honesty gates (WP-A1–A11, G6) | `export_runtime_baseline.py`, `ci.yml`, `lint_claims.py` | **ОТКРЫТЬ PR** | Блокер Day-1 §2 до merge + CI artifact | 09.08 |
| — | остальные open PR | — | **ТРЕБУЕТСЯ `gh auth login`** | Таблица 7 PR — после auth | 09.08 EOD |

Открыть PR вручную (без gh):

https://github.com/KonkovDV/AeroBIM/compare/main...red-team/wp-a-p0-gates?expand=1

Заголовок: `feat(red-team): P0 honesty gates WP-A1–A11 + G6 claim boundary`

После merge + green CI: скачать artifact `ci-runtime-baseline` → закоммитить в `docs/evidence/runtime-baseline-latest.json` (`attested_by: ci`, `publishable: true`).

```bash
gh auth login
gh pr list --state open --json number,title,headRefName,updatedAt,mergeable
```

После P0-merge: ребейз каждого PR на `red-team/wp-a-p0-gates` или закрытие как устаревший, если затрагивает `runtime-baseline-latest.json`, `ENGINEERING_STATUS`, README snippets.
