# PR triage — 2026-08-09 (G4 / N-31) — обновление после merge #12

**Дата пересверки:** 2026-08-09  
**Команды:**

```text
gh auth status          → KonkovDV (OK)
gh pr list --state open → []
git branch -a           → только main (+ эта feature-ветка до merge)
```

| PR / ветка | Решение | Обоснование |
|---|---|---|
| **#12** `fix/publishable-circular-lock-n24-n26` | **MERGED** (`5a75d86`) | Циклический lock publishable + N-24/25/26 |
| `red-team/wp-a-p0-gates` | **DELETED** | 0 ahead / 4 behind; работа ушла в main через #12 |
| «7 открытых PR» из промта 01:14 | **SUPERSEDED** | На дереве `5a75d86` open PR = 0 |
| **NEW** `red-team/v5-p0-closure` | **ОТКРЫТЬ** | WP-A1b CI baseline generation fix + N-27/28/29 + K1 §7 + audit map |

**Политика (И12 / N-31):** прямой push в `main` без PR запрещён. Доказательство прогона — `run_id` CI на PR.

**Следующий шаг после зелёного CI этой ветки:** скачать `ci-runtime-baseline` с `publishable: true` → отдельный PR «chore(evidence): commit CI-attested baseline (WP-A1b)».
