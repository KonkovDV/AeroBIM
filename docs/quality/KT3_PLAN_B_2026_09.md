---
title: "План Б — КТ#3 без ответа Самолёта: метод, локальный замер, дисциплина запросов"
status: active
version: "1.0.0"
last_updated: "2026-09-03"
claim_boundary: "Сценарий КТ#3 при отсутствии ответов D1–D2. Все числа — fixture/synthetic-only. Checkpoint NO_GO; RT-001/002/003 OPEN. Не закрывает RT и не является точностью."
---

# План Б — КТ#3 без ответа Самолёта

## 1. Нарратив (что говорим)

1. **Метод**: полный сквозной путь — комплект → intake-гейты → детерминированные проверки → находки с доказательствами → эксперт (HITL) → BCF/HTML/JSON → повторный прогон с дельтой ревизий. Все экраны и все честные маркеры показываются как есть.
2. **Локальный замер**: протокол до/после с чередованием проходов; на КТ#3 предъявляется журнал и tool-side измерения; ручная сторона — по протоколу, дата назначена.
3. **Дисциплина запросов**: запрос B1–B7/C1–C3/D1–D2 отправлен 31.08 с дедлайном 08.09; показываем сам запрос, его формулировки и пустые ячейки как работу, а не как оправдание.
4. **Честная рамка**: NO_GO не перекрашивается; «любой из RT-001…003, поданный как выполненный, — автоматический NO_GO».

## 2. Исполненное к 03.09 (артефакты, готовые к показу)

| Артефакт | Путь / команда | Состояние |
|---|---|---|
| Синтетический чистый пакет | `.local/e2-clean-pack` (`generate_synthetic_package`) | 4 файла, 7 мутаций SSOT, `synthetic_only` |
| **Инъекция дефектов E2.1–5** | `inject_defects --seed 20260824` + `evaluate_injection_recall` | дом-5 0/6; mini-IFC 1/8; Wilson lower 0.000 / 0.022; `synthetic_only` |
| SIG-01 канал-прогон | `.local/pack-out/sig01-rerun-2026-08-31/` | EXECUTED, `pack_volume_not_accuracy` |
| Лабораторный журнал | `docs/evidence/lab-before-after-fixture-tool-only-latest.json` | tool-side есть; `t_manual_s: null` |
| Мастер-реестр пакета | `.local/pack-out/per-file-registry/` | 5312/5312 файлов с фактами |

## 3. Runbook до демо (по убыванию)

1. **E1 manual pass (человек, ~1 час):** заморозить фикстуру (хеш в журнал) → проход «без системы» с секундомером (протокол замечаний: суть+пункт+ось/этаж) → проход «с системой» на том же freeze → вписать `t_manual_s` в журнал → чередовать порядок на втором прогоне.
2. **E2 шаги 3–5 (исполнено 03.09):** `evaluate_injection_recall.py` склеил манифест с analyze через CONTROL-дифф. Дом-5: 0/6 (Wilson lower 0.000). Mini-IFC: 1/8 (Wilson lower 0.022, убит только `MISSING_ELEMENT`). `claim_level=synthetic_only`. Запись: [`../evidence/DEFECT_INJECTION_RECALL_RUN_2026_09.md`](../evidence/DEFECT_INJECTION_RECALL_RUN_2026_09.md). Не закрывает RT-001.
3. **Демо-маршрут одним кликом** + таймер (checklist A4) + RBAC-минимум (A1).
4. **Слайды**: «пустые данные пакета → запрос B1–B7» и «учебный набор правил» — обязательные.

## 4. Чего этот план НЕ делает

- Не создаёт customer-precision (D1–D2 остаются у Самолёта); `confirmed_partner_validation_metrics() == False`.
- Не перекрашивает NO_GO; не заявляет порог точности ТЗ, лимит времени прогона, «MEP», «DWG».
- Recall на синтетике не переносится на комплект Самолёта (записано в плане инъекций).

## 5. Если ответ придёт после 08.09

План Б не выбрасывается: замер и recall-артефакты становятся «методической секцией», а данные Самолёта подключаются сверху — инвентарь и манифесты уже готовы (43 ГБ → 5312 файлов реестра), подключение = прогон на пакете + adjudication.
