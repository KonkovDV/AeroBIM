# IFC-Bench v2 pins

See [`IMPORT_PINS.json`](IMPORT_PINS.json) for hashes, licenses, and GPLv3 exclusions.

Checkout (gitignored): `.local/ifc-bench` via `AEROBIM_IFC_BENCH_ROOT`.  
Do not vendor the full ~2 GB tree into `samples/` until selective non-GPL pull is approved.
