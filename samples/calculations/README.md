# Calculation fixtures

Synthetic calculation inputs (TZ row 10). Fixture сверка only — not independent
structural verification (TZ row 11 / OpenRebar).

- `area-requirement.txt` — narrative area / fire-rating lines
- `techlab-area-calc.txt` — free-text area notes
- `load-table.txt` — `LOAD|id|label|expected|unit|observed` rows
- `lira-declared-vs-rd.json` — declared-field table compare fixture (xlsx/docx path in tests; not a solver)
- `openrebar-slab-03.result.json` — OpenRebar provenance сверка (not correctness)

Not customer calculation volumes.