# Typical errors — pointers

Canonical scaffold: [`../../benchmarks/samolet-typical-errors-catalog.json`](../../benchmarks/samolet-typical-errors-catalog.json).
Populate with customer-confirmed patterns during intake.

2026-08-28: two acceptance checklists detected in the owner pack (counts only; names stay out of git). Not ingested; `customer_confirmed_patterns` stays 0. The catalog is not customer-accepted.
