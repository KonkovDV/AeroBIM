# Project documentation (PD) — pointers

See parent [`MANIFEST.json`](../MANIFEST.json) appendix `01-project-docs`.
Place customer PD exports here only if cleared for git; otherwise keep under local operator paths.
