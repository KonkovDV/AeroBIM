# Sprint-era CLI runners (outside the active operator catalog)

These scripts are thin launchers. Implementation stays importable as
``aerobim.tools.run_sprint*`` so existing tests keep working.

Active operator tools (≤40) are listed by ``python -m aerobim.tools``.
Do not treat sprint runners as product capabilities.
