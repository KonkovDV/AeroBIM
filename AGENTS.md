# AeroBIM — IFC / IDS construction-pack checker

Checkpoint `GO` is the regulatory-measurement MVP (public examination IDS, fixture gold, planted clash, HVAC IfcSystem rehearsal, channel EIR/NWD carriers). `customer_go` stays false until residual volumes close: two human raters on a pack-specific corpus, a named appointing-party signature for *deployment*, system MEP or customer federated IFC, and CDE proof. Measurement substitutes (public IDS, RF typical-error catalogs, planted IfcClash, simulated dual-rater protocol rehearsal) do not make `PrecisionClaim.publishable` or MEP delivered. Simulated raters are not humans. LLM is not a rater. Not a CDE, not a model viewer, not an expert replacement.
Do not claim product accuracy >90%, customer SLA, native DWG, MEP delivered, or production-ready.
LLM/VLM never write `summary.passed` (ADR-001).

Internal working notes live outside this repository and must not be committed.
